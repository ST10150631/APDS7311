const express = require("express");
const mongoose = require("mongoose");
const db = require("../db/conn"); // Assuming this connects to MongoDB properly
const router = express.Router();



module.exports = router;